# Legacy example file (not auto-loaded by Spring Boot).
# Prefer configuring values in src/main/resources/application.properties
# and profile-specific files such as application-prod.properties.

spring.application.name=Sales App
spring.datasource.url=jdbc:postgresql://localhost:5432/sales
spring.datasource.username=postgres
spring.datasource.password=change-me
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
